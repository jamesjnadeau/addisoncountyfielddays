{
  "name": "addisoncountyfielddays",
  "version": "1.00",
  "description": "A static website for Addison County Fair and Field Days.",
  "type": "module",
  "engines": {
    "node": ">=22.x"
  },
  "scripts": {
    "start": "npx @11ty/eleventy --config .eleventy.js --serve --port=8081",
    "dev": "NODE_ENV=development npm run start",
    "build": "npx @11ty/eleventy --config .eleventy.js"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-plugin-pug": "^1.0.0",
    "bootstrap": "^5.3.3",
    "npm-run-all": "*",
    "sass": "^1.85.1"
  },
  "license": "MIT"
}
